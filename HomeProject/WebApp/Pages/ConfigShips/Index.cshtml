@page
@model WebApp.Pages_ConfigShips.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfigShip![0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigShip![0].Config)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfigShip![0].Ship)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ConfigShip!) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Config!.ConfigStr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ship!.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ConfigShipId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ConfigShipId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ConfigShipId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
