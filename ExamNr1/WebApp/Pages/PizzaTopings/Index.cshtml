@page
@model WebApp.Pages_PizzaTopings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PizzaToping[0].Pizza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PizzaToping[0].Topping)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PizzaToping) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pizza!.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topping!.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
